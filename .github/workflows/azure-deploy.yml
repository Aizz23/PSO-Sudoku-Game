name: CI/CD Pipeline - Test, Build, Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  AZURE_REGISTRY_LOGIN_SERVER: sudokustrupwa.azurecr.io
  AZURE_RESOURCE_GROUP: sudoku-rg
  BACKEND_IMAGE_NAME: sudoku-backend
  FRONTEND_IMAGE_NAME: sudoku-frontend
  BACKEND_APP_NAME: sudoku-backend
  FRONTEND_APP_NAME: sudoku-frontend
  NODE_VERSION: '20'
  
jobs:
  # ═══════════════════════════════════════════════════════
  # STAGE 1: TESTING (Quality Gate)
  # ═══════════════════════════════════════════════════════
  
  test-backend:
    name: 🧪 Test Backend
    runs-on: ubuntu-latest

    services:
      mongodb: 
        image: mongo:7
        ports:
          - 27017:27017
        options:  >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: package-lock.json
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🧪 Run tests
      run: npm test --workspace=backend
      env:
        MONGODB_URI_TEST: mongodb://localhost:27017/sudoku-test
        NODE_ENV: test
      
    - name: ✅ Backend tests passed
      run: echo "Backend tests completed successfully!"
  
  test-frontend:
    name: 🧪 Test Frontend
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: package-lock.json
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🧪 Run tests
      run: npm test --workspace=frontend -- --coverage --watchAll=false
      
    - name: 🏗️ Build check
      run: npm run build --workspace=frontend
      
    - name: ✅ Frontend tests passed
      run: echo "Frontend tests completed successfully!"
  
  # ═══════════════════════════════════════════════════════
  # STAGE 2: BUILD & PUSH (Only if tests pass)
  # ═══════════════════════════════════════════════════════
  
  build-backend:
    name: 🏗️ Build and Push Backend
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: 🔐 Log in to Azure Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}
    
    - name: 🏗️ Build and push Backend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        push: true
        tags: |
          ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.BACKEND_IMAGE_NAME }}:${{ github.sha }}
          ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.BACKEND_IMAGE_NAME }}:latest
        cache-from: type=gha,scope=backend
        cache-to: type=gha,mode=max,scope=backend
    
    - name: 🔍 Scan Backend image for vulnerabilities
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.BACKEND_IMAGE_NAME }}:${{ github.sha }}
        format: 'sarif'
        output: 'trivy-backend-results.sarif'
        severity: 'CRITICAL,HIGH'
    
    - name: 📤 Upload Backend scan results
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: 'trivy-backend-results.sarif'
        category: 'backend-image'
    
    - name: 📝 Backend Image Info
      run: |
        echo "✅ Backend image pushed successfully!"
        echo "📦 Tags:"
        echo "  - ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.BACKEND_IMAGE_NAME }}:${{ github.sha }}"
        echo "  - ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.BACKEND_IMAGE_NAME }}:latest"

  build-frontend:
    name: 🏗️ Build and Push Frontend
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: 🔐 Log in to Azure Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}
    
    - name: 🏗️ Build and push Frontend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        push: true
        build-args: |
          REACT_APP_API_URL=https://sudoku-backend.agreeableforest-82817a2d.southeastasia.azurecontainerapps.io
        tags: |
          ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.FRONTEND_IMAGE_NAME }}:${{ github.sha }}
          ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.FRONTEND_IMAGE_NAME }}:latest
        cache-from: type=gha,scope=frontend
        cache-to: type=gha,mode=max,scope=frontend
    
    - name: 🔍 Scan Frontend image for vulnerabilities
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.FRONTEND_IMAGE_NAME }}:${{ github.sha }}
        format: 'sarif'
        output: 'trivy-frontend-results.sarif'
        severity: 'CRITICAL,HIGH'
    
    - name: 📤 Upload Frontend scan results
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: 'trivy-frontend-results.sarif'
        category: 'frontend-image'
    
    - name: 📝 Frontend Image Info
      run: |
        echo "✅ Frontend image pushed successfully!"
        echo "📦 Tags:"
        echo "  - ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.FRONTEND_IMAGE_NAME }}:${{ github.sha }}"
        echo "  - ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.FRONTEND_IMAGE_NAME }}:latest"

  # ═══════════════════════════════════════════════════════
  # STAGE 3: DEPLOYMENT INSTRUCTIONS
  # ═══════════════════════════════════════════════════════

  deployment-summary:
    name: 🎉 Pipeline Complete
    needs: [build-backend, build-frontend]
    runs-on: ubuntu-latest
    
    steps:
    - name: 📝 Show Deployment Commands
      run: |
        echo "╔════════════════════════════════════════════════════════╗"
        echo "║  🎉 CI/CD PIPELINE COMPLETED SUCCESSFULLY!            ║"
        echo "╚════════════════════════════════════════════════════════╝"
        echo ""
        echo "✅ Stage 1: Tests Passed"
        echo "✅ Stage 2: Docker Images Built & Pushed to ACR"
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "📦 DOCKER IMAGES READY:"
        echo "  Backend:  ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.BACKEND_IMAGE_NAME }}:latest"
        echo "  Frontend: ${{ env.AZURE_REGISTRY_LOGIN_SERVER }}/${{ env.FRONTEND_IMAGE_NAME }}:latest"
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🚀 NEXT STEP: Deploy to Azure (Manual)"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "# Option 1: Deploy All (Backend + Frontend)"
        echo "./deploy-all.sh"
        echo ""
        echo "# Option 2: Deploy Individual Services"
        echo "./deploy-backend.sh"
        echo "./deploy-frontend.sh"
        echo ""
        echo "# Option 3: Manual Azure CLI"
        echo "az containerapp update \\"
        echo "  --name sudoku-backend \\"
        echo "  --resource-group sudoku-rg \\"
        echo "  --image sudokustrupwa.azurecr.io/sudoku-backend:latest"
        echo ""
        echo "az containerapp update \\"
        echo "  --name sudoku-frontend \\"
        echo "  --resource-group sudoku-rg \\"
        echo "  --image sudokustrupwa.azurecr.io/sudoku-frontend:latest"